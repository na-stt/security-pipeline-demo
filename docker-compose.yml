services:
  web:
    build: .
    environment:
      MONGODB_URI: mongodb://mongo:27017/nodegoat
    ports: ["127.0.0.1:4000:4000"]
    depends_on: [mongo]
  mongo:
    image: mongo:8.0
    volumes: [demo-db:/data/db]
volumes:
  demo-db:
